{
  "name": "agentdb",
  "private": true,
  "version": "0.1.0",
  "packageManager": "bun@1.2.19",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "scripts": {
    "build": "bun run --filter '*' --if-present build",
    "check": "bun run --filter '*' --if-present check"
  }
}
