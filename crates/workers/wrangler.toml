name = "statecraft"
main = "build/worker/shim.mjs"
compatibility_date = "2024-12-01"

[build]
command = "cargo install worker-build && worker-build --release"

# D1 database binding — create with:
#   wrangler d1 create statecraft
[[d1_databases]]
binding = "STATECRAFT_DB"
database_name = "statecraft"
database_id = ""  # Fill in after `wrangler d1 create`

# Environment variables
[vars]
STATECRAFT_DEV_MODE = "false"

# Secrets (set via `wrangler secret put`):
#   STATECRAFT_ADMIN_TOKEN
#   STATECRAFT_AI_API_KEY (optional)

# Durable Objects for WebSocket rooms (future)
# [durable_objects]
# bindings = [
#   { name = "ROOM", class_name = "RoomDO" }
# ]

# Cron triggers for scheduled jobs (future)
# [triggers]
# crons = ["*/5 * * * *"]
