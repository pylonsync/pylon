[package]
name = "agentdb-policy"
edition.workspace = true
license.workspace = true
version.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
agentdb-core = { path = "../core" }
agentdb-auth = { path = "../auth" }
serde_json = { workspace = true }

