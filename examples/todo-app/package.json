{
  "name": "@statecraft/example-todo-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@statecraft/sdk": "workspace:*"
  },
  "scripts": {
    "generate-manifest": "bun run app.ts",
    "check": "tsc -p tsconfig.json --noEmit"
  }
}
